var sinon = require('sinon');
var App = require('../src/app');

describe('App', function() {
  describe('test sendFile', function () {

    it('creates connections correctly', function() {

    });
  });
});
